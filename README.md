# 🔧 ML Hyperparameter Tuning App

This is a Streamlit web app to train, tune, and compare machine learning models using GridSearchCV and RandomizedSearchCV.

## Run Locally

```bash
pip install -r requirements.txt
streamlit run app.py
